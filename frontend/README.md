# Notebook — Frontend

React + Vite frontend for the Notes App.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Runs on http://localhost:3000 by default. Expects the backend API running at the URL set in `VITE_API_BASE_URL`.